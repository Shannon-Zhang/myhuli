// 增加new组件banner数据
export function addBannerList(state, option) {
    state.bannerList=option;
}

// 增加newAry中的数据
export function addNewAry(state, option) {
    state.newAry=option;
}
// 增加newAry2中的数据
export function addNewAry2(state, option) {
    state.newAry2=option;
}

// 增加profile组件value_item数据
export function addProfileList(state, option) {
    state.profileList=option;
}

// 增加manage组件manageList数据
export function addManageList(state, option) {
    state.manageList=option;
}

// 增加mediareport组件mediaList数据
// export function addMediaList(state, option) {
//     state.mediaList=option;
// }

// 增加home组件bannerList数据
export function addHomeBannerList(state, option) {
    state.homeBannerList=option;
}

// 增加home组件contentList数据
export function addHomeContentList(state, option) {
    state.homeContentList=option;
}